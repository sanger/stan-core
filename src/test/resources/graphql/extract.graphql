mutation {
    extract(request: {barcodes: [], labwareType: "LWTYPE", sasNumber: "SAS4000"}) {
        labware {
            id
            barcode
            slots {
                samples {
                    id
                    tissue {
                        externalName
                    }
                }
            }
            labwareType { name }
        }
        operations {
            id
            operationType { name }
            performed
            actions {
                sample {
                    id
                }
                destination {
                    labwareId
                    address
                    samples {
                        id
                    }
                }
                source {
                    labwareId
                    address
                }
            }
        }
    }
}