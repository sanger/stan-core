mutation {
    recordPerm(request: {
        barcode: "STAN-50"
        permData: [
            {
                address: "A1"
                seconds: 124
            }
        ]
    }) {
        operations {id}
    }
}
