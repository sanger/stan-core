query {
    planData(barcode: "$BARCODE") {
        plan {
            operationType { name }
            planActions {
                destination {
                    labwareId
                    address
                }
                sectioningOrder
                source { address }
                sample { id }
            }
        }
        destination {
            labwareType { name }
            barcode
        }
        sources {
            barcode
        }
        groups
    }
}