query {
    planData(barcode: "$BARCODE") {
        plan {
            operationType { name }
        }
        destination {
            barcode
        }
        sources {
            barcode
        }
    }
}