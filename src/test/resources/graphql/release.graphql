mutation {
    release(request: {
        barcodes: []
        destination: "DESTINATION"
        recipient: "RECIPIENT"
    }) {
        releases {
            destination { name }
            recipient { username }
            labware { barcode, released }
        }
    }
}
