mutation {
    addExternalID(request: {
        labwareBarcode: "Barcode1"
        externalName: "ExternalName"
    }) {
        operations {
            operationType { name }
            id
        }
    }
}