mutation {
    unrelease(request: {
        labware: [{
            barcode: "BARCODE"
        }]
    }) {
        labware {
            state
        }
        operations {
            id
            operationType { name }
        }
    }
}
