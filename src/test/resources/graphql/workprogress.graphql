query {
    workProgress(workNumber: "SGP500", statuses: [active]) {
        work { workNumber, workTypes { name }}
        timestamps { type, timestamp }
        mostRecentOperation
        workComment
    }
}